<?php
/**
 * Header Layout Template
 * File  : app/code/Page/View/header.phtml
 * Block : Page_Block_Header
 *
 * This file is the layout shell only.
 * It delegates all rendering to three child blocks:
 *
 *   getChildHtml('menu')   → Page_Block_Header_Menu   → header/menu.phtml
 *   getChildHtml('search') → Page_Block_Header_Search → header/search.phtml
 *   getChildHtml('icon')   → Page_Block_Header_Icon   → header/icon.phtml
 *
 * No session logic, no data arrays, no business logic here.
 */
?>

<!-- =====================================================
     SITE HEADER
     ===================================================== -->
<header class="site-header" id="site-header" role="banner">
    <div class="header-inner">

        <!-- ── Logo (static branding — no block needed) ── -->
        <a href="/" class="header-logo" id="header-logo" aria-label="MVC1 Store — Home">
            <span class="logo-icon" aria-hidden="true">M</span>
            <span class="logo-text">MVC1 Store</span>
        </a>

        <!-- ── Search Bar ── (Page_Block_Header_Search) -->
        <?php $this->getChildHtml('search'); ?>

        <!-- ── Navigation Links ── (Page_Block_Header_Menu) -->
        <?php $this->getChildHtml('menu'); ?>

        <!-- ── Profile / Dropdown Icon ── (Page_Block_Header_Icon) -->
        <?php $this->getChildHtml('icon'); ?>

        <!-- ── Hamburger Button (mobile) ── -->
        <button
            class="hamburger"
            id="hamburger-btn"
            type="button"
            aria-expanded="false"
            aria-controls="mobile-nav-menu"
            aria-label="Toggle mobile menu"
        >
            <span></span>
            <span></span>
            <span></span>
        </button>

    </div><!-- /.header-inner -->
</header><!-- /.site-header -->

<!--
    NOTE: The mobile-nav overlay is rendered by menu.phtml (Page_Block_Header_Menu)
    so it shares the same dynamic link data as the desktop nav.
-->

<!-- =====================================================
     HEADER JAVASCRIPT
     Dropdown toggle | Hamburger | Click-outside | Search
     ===================================================== -->
<script>
(function () {
    'use strict';

    /* ── Element references ── */
    var profileBtn   = document.getElementById('profile-toggle-btn');
    var dropdown     = document.getElementById('profile-dropdown-menu');
    var hamburgerBtn = document.getElementById('hamburger-btn');
    var mobileNav    = document.getElementById('mobile-nav-menu');
    var searchBtn    = document.getElementById('header-search-btn');
    var searchInput  = document.getElementById('header-search-input');

    /* ── Profile Dropdown ── */
    function openDropdown() {
        if (!dropdown) return;
        dropdown.classList.add('is-open');
        profileBtn.setAttribute('aria-expanded', 'true');
    }

    function closeDropdown() {
        if (!dropdown) return;
        dropdown.classList.remove('is-open');
        profileBtn.setAttribute('aria-expanded', 'false');
    }

    if (profileBtn && dropdown) {
        profileBtn.addEventListener('click', function (e) {
            e.stopPropagation();
            dropdown.classList.contains('is-open') ? closeDropdown() : openDropdown();
        });

        document.addEventListener('keydown', function (e) {
            if (e.key === 'Escape') { closeDropdown(); closeMobileNav(); }
        });

        document.addEventListener('click', function (e) {
            var profileWrapper = document.getElementById('header-profile');
            if (profileWrapper && !profileWrapper.contains(e.target)) {
                closeDropdown();
            }
        });
    }

    /* ── Hamburger / Mobile Nav ── */
    function openMobileNav() {
        if (!mobileNav) return;
        mobileNav.classList.add('is-open');
        hamburgerBtn.classList.add('is-open');
        hamburgerBtn.setAttribute('aria-expanded', 'true');
        mobileNav.setAttribute('aria-hidden', 'false');
        document.body.style.overflow = 'hidden';
    }

    function closeMobileNav() {
        if (!mobileNav) return;
        mobileNav.classList.remove('is-open');
        hamburgerBtn.classList.remove('is-open');
        hamburgerBtn.setAttribute('aria-expanded', 'false');
        mobileNav.setAttribute('aria-hidden', 'true');
        document.body.style.overflow = '';
    }

    if (hamburgerBtn && mobileNav) {
        hamburgerBtn.addEventListener('click', function () {
            mobileNav.classList.contains('is-open') ? closeMobileNav() : (openMobileNav(), closeDropdown());
        });
    }

    /* ── Search — reads action URL from data-action attribute ── */
    if (searchBtn && searchInput) {
        function doSearch() {
            var q      = searchInput.value.trim();
            var action = searchInput.getAttribute('data-action') || '/catalog/product';
            if (q.length > 0) {
                window.location.href = action + '?q=' + encodeURIComponent(q);
            }
        }
        searchBtn.addEventListener('click', doSearch);
        searchInput.addEventListener('keydown', function (e) {
            if (e.key === 'Enter') doSearch();
        });
    }

    /* ── Active nav link highlight ── */
    var currentPath = window.location.pathname;
    document.querySelectorAll('.header-nav .nav-link, .mobile-nav .nav-link').forEach(function (link) {
        var href = link.getAttribute('href');
        if (!href) return;
        if (href === '/' && currentPath === '/') {
            link.classList.add('active');
        } else if (href !== '/' && currentPath.indexOf(href) === 0) {
            link.classList.add('active');
        }
    });

}());
</script>